#! /usr/bin/env bash

if [[ $# != 0 ]]
then
  echo "Usage: $(basename $0) <overlay>"
  echo ""
  echo "Apply the given overlay for the current kubectl context."
  exit 1
fi

kubectl apply -k k8s/$(kubectx -c) | grep -v unchanged
